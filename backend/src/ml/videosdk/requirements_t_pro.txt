# Зависимости для модели T-pro-it-1.0 от T-Tech
# https://huggingface.co/t-tech/T-pro-it-1.0

# Основные зависимости
torch>=2.0.0
transformers>=4.35.0
tokenizers>=0.14.0

# Опциональные зависимости для оптимизации
vllm>=0.2.0  # Для оптимизации производительности
accelerate>=0.20.0  # Для ускорения загрузки модели
bitsandbytes>=0.41.0  # Для квантизации

# Зависимости для работы с VideoSDK
videosdk>=1.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
sqlalchemy>=2.0.0
pydantic>=2.0.0

# Дополнительные утилиты
huggingface-hub>=0.16.0
numpy>=1.24.0
scipy>=1.10.0

# Для тестирования
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Для логирования и мониторинга
loguru>=0.7.0
tqdm>=4.65.0

# Для работы с русским языком
pymorphy3>=2.0.0  # Морфологический анализ
nltk>=3.8.0  # Обработка естественного языка

# Системные требования:
# - Python 3.8+
# - CUDA 11.8+ (для GPU ускорения)
# - Минимум 16GB RAM (рекомендуется 32GB+)
# - Минимум 8GB VRAM (для GPU версии)
